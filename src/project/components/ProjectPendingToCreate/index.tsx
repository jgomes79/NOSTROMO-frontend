import { Fieldset } from "../../../shared/components/Fieldset";

export const ProjectPendingToCreate = () => {
  return (
    <Fieldset title={"Voting Phase"} variant={"white"}>
      asda
    </Fieldset>
  );
};
